export {}
/*
export const createColumn = (
    header: React.ReactNode,
    accessorKey: string
): ColumnDef<BladeTaskQuery> => {
    return {
        header: ({ column }) => {
            return (
                <button
                    onClick={() =>
                        column.toggleSorting(column.getIsSorted() === "asc")
                    }
                >
                    {header}
                </button>
            );
        },
        accessorKey: accessorKey,
    };
};

export const columnBT: ColumnDef<BladeTaskQuery>[] = [
    createColumn("BladeProject ID", "bladeProject.id"),
    createColumn("BladeTask Name", "taskName"),
    createColumn("Test Type", "testType"),
    createColumn("Start Date", "startDate"),
    createColumn("End Date", "endDate"),
    createColumn("Duration", "duration"),
    createColumn("Attach Period", "attachPeriod"),
    createColumn("Detach Period", "detachPeriod"),
    createColumn("Test Rig", "testRig"),
];*/